{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "cwd": "${workspaceFolder}",
            "args": ["go run main.go"],
            "program": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}